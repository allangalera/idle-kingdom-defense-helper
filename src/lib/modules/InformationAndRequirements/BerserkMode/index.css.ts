import { theme } from '$lib/styles/themes/index.css';
import { style } from '@vanilla-extract/css';

export const effectsHeader = style({
  gridColumn: 'span 6',
});

export const doubleRow = style({
  gridRow: 'span 2',
});
